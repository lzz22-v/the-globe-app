// index.js (Arquivo na RAIZ do seu projeto)

import { registerRootComponent } from 'expo';

import App from './App';

// registerRootComponent é o ponto de entrada da aplicação
registerRootComponent(App);